cmake_minimum_required(VERSION 3.16.0)

# This top-level CMakeLists.txt is only used for the examples under examples/.
# Downstream projects consume components via list(APPEND EXTRA_COMPONENT_DIRS ...).

message(STATUS "lib-tbd library — no examples configured yet")
