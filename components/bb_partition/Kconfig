menu "breadboard: bb_partition"

config BB_PARTITION_ROUTES_AUTOREGISTER
    bool "Auto-register /api/diag/partitions route via bb_init"
    default y
    help
        When enabled, bb_partition registers GET /api/diag/partitions
        automatically via the bb_init regular tier after the HTTP server
        starts.

endmenu
