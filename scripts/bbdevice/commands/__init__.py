"""First-party plugin list — imported by cli.py to load built-ins onto the bus.

Empty today — the device-testing command set lands in a later PR. Once
commands exist, import each module here and append it to FIRST_PARTY,
mirroring scripts/bbtool/commands/__init__.py.
"""
FIRST_PARTY = []
